<section class="search">
  <img src="/images/{{search.filename}}" alt="{{search.description}}" />
  <div class="search-details">
    <h3>{{search.title}}</h3>
    <p>RESULTS:{{search.allergen}}</p>
  </div>
</section>