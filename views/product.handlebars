<section class="product">
  <img src="/images/{{product.product_image}}"/>
  <div class="product-details">
    <h3>{{product.name}}</h3>
    <p>Allergen type:{{allergen.allergen_name}}</p>
  </div>
</section>