<section class="product">
  <img src="/images/{{product.filename}}" alt="{{product.description}}" />
  <div class="product-details">
    <h3>{{product.title}}</h3>
    <p>Allergen type:{{product.allergen}}</p>
  </div>
</section>