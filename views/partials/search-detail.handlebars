<section class="card">
  <a href="/search/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
      <img src="{{product_image}}" alt="{{description}}" />
      <p>CONTAINS: {{allergen}}</p>
      <div class="form-group">
        <button class="btn btn-primary" type="click">SAVE</button>
      </div>
    </div>
  </a>
</section>