<section class="card">
  <a>
    <header>{{name}}</header>

    <div class="card-body">
      <img src="{{product_image}}" alt="{{description}}" />
      <p>{{description}}</p>
      <p>CONTAINS: {{allergen}}</p>
      <div class="form-group">
        <button class="btn btn-primary sav-btn" type="click" id='{{index}}'>SAVE</button>
      </div>
    </div>
  </a>
</section>